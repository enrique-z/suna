Coletilla prd (de AI JAson)

**I AM BUILDING XXXXX  Help me think through how should i build this, and how should i structure the PRD and tech stack , think step by step, for engineers;
#PRD should have section:** 

**#Project overview** 

**#Features** 

**#Requirements for each feature**

**#Data Models**

**#API Contract**

**Be explicit about dependencies, variable names, api to call, etc, so it leaves no ambiguities**



----coletilla 2. al final de cada instruccion para que no borre-----------
**cursor.app can not handle it. so please rewrite it giving simple tasks one by one or it is deleting many of the old format and code and many of the old good functionalities. becasue i tried to give him too much new content and it mess it up.**

**rewrite it so i give him only one small task as a time. and check if he is doing it correctly, instead of letting him create so many files and at the end update the file at the end, this approach will fail.**

**write all as to ai ide editor cursor, so i just copy past in the composer of cursor. write only for him.  not for me**

**you should also tell him that he doesnt need to modify other parts, because the errors i have now is because he changed other parts that were wroking perfectly and now dont, and even he changed the formats and everythign because i didnt tell cursor not to modify others**

----estea coletilla es paa claude app no cursor ---
para claude coletilla para que no repita
CONTINUE WRITING from the last line you stopped. dont rewrite again what you already wrote. only continue writing the rest missing part of the document
-------
---coletilla para cursor para que no cometa errores iguales ---
Now reflect the errors you made, and create a new cursor rules to make sure you dont make those mistakes again


--coletilla prompt 3 ---------------
# IMPORTANT INSTRUCTIONS FOR CURSOR:

# File Location: XXX

# STEP 1: DO NOT REPLACE OR DELETE ANY EXISTING CODE

# Instead, find the generate_detailed_report function and ADD these lines

# inside the existing try block, BEFORE the return statement:

```
    # Add these lines 
    xxxxxx

```

# VERIFICATION STEPS:

# 1. Verify ALL existing code remains unchanged

# 2. Confirm the new sections are ADDED to existing report

# 3. Check that timestamps and other functionality still work

# 4. Verify all existing data is preserved

# 5. Test that new analysis appears in both display and download

# WARNING:

# DO NOT delete ANY existing code

# DO NOT modify ANY existing functions

# DO NOT change ANY existing report structure

# ONLY ADD new content

# If anything was deleted, RESTORE it immediately

# If you see any other changes, REVERT them

# SUCCESS CRITERIA:

# The  new functions are added correctly

# All existing functionality works as before

# New functions can be called independently

# Proper error handling for trend analysis

# For now, just add them without modifying any existing functionality.
